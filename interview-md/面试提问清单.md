# 面试提问清单

## 项目总览

- 电商项目有哪些功能，怎么实现的？

## Redis / 秒杀 / 并发

- 秒杀系统为什么要用 Redis 承接高并发入口？
- Lua 脚本里具体校验哪些条件？
- 库存怎么判断？
- 一人一单怎么判断？
- 除了 Redis 校验外，数据库里有没有兜底约束？
- 唯一索引建在哪里？
- 校验通过后订单如何异步落库？
- Redis Stream 在这里起什么作用？
- MySQL 最终兜底是怎么做的？
- 唯一索引的 key 由哪些字段组成？
- 用户重复点击或并发下单时，如何保证只生成一单且不超卖？（比如100人同时抢购，但是库存里只有10张优惠卷）
- 库存是先写入 Redis 还是数据库？
- 下单后库存扣减和异步落库失败怎么处理？
- Redis Stream 消费失败后怎么重试？
- Pending List 或死信队列怎么处理？
- 怎么保证读写一致性？
- 是否有加锁或其他并发控制？
- 缓存和数据库的数据同步/缓存更新策略是什么？
- Redis 是怎样查找 MySQL 数据的？
- 如果 Redis 查不到或发生缓存穿透怎么办？
- 是否用了空值缓存、随机过期时间或缓存更新？
- 项目里用 RedisTemplate 或相关模板做了哪些 Redis 读写操作？
- 数据存储上用缓存还是 Redis？

## MySQL / 数据库设计

- 这个电商项目有哪些核心数据表？
- 用户表、优惠券表、商铺表、订单表等怎么设计？
- MySQL 背后的存储引擎是什么？
- 知道对象存储是怎么实现的？比如视频、文件、图片等。

## HTTP / 网络 / 后端基础

- 讲一讲 HTTP 协议。
- HTTP 接口是怎么做的？
- 请求格式是不是 RESTful 风格？
- GET、POST 这类请求方式怎么理解？
- 请求参数怎么传？
- Cookie 和 Session 有什么区别？
- 什么场景用 Cookie，什么场景用 Session？
- HTTP 状态码怎么分类？
- 2xx、3xx、4xx、5xx 分别表示什么？
- 用户态和内核态有什么区别？
- 你写的 Java 代码什么时候处于用户态，什么时候进入内核态？
- 讲一讲 TCP。
- TCP 用什么机制保证可靠连接？
- TCP 和 UDP 有什么区别？

## Java / Redis 数据结构

- 数组和链表有什么区别？
- Java 线程同步怎么做？
- 讲一讲 Redis 数据结构 ZSET。
- ZSET 底层的实现原理是什么？

## AI 求职平台 / 简历解析

- 你这个 AI 求职平台从需求到实现是怎么拆分的？
- 第一阶段先做什么？
- 技术选型怎么做？
- 为什么选择 Python？
- 哪些功能先做，哪些功能先不做？
- 接入大模型时怎么选型？
- 用国内模型还是国外模型？
- 为什么选择这些模型？
- 使用 AI 编程工具时，你是怎么给模型下指令的？
- 是否会指定角色、任务和输出格式？
- 简历解析具体怎么做？
- 大模型输出的结构化 JSON 包含哪些字段？
- 岗位匹配或岗位评分怎么实现？
- 岗位描述从哪里来？
- 如何把岗位描述和简历做匹配？
- AI 帮你实现代码后，你怎么验收？
- 不满意时怎么修改提示词或迭代功能？
- 模拟面试功能有没有做过？
- 运动、步数、排行榜这类功能有没有做过？
- 这些功能的数据怎么记录？

## 接口设计 / 用户关系 / 关注功能

- 用户关注和粉丝关系怎么存储？
- 表结构怎么设计？
- 用户表里是否应该用字段存关注者 ID？
- 这个表应该有几个字段？
- 关注功能接口怎么设计？
- 共同好友功能接口怎么设计？
- 设计一个类似微信运动的平台需要哪些接口？
- 如何查询“我关注的人”和“关注我的人”？
- 互相关注或共同关注的判断条件是什么？
- 步数/运动数据与关注关系结合时，应该返回哪些数据？
